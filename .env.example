# Example environment configuration
# Copia: cp .env.example .env  y ajusta valores.

# PostgreSQL database settings
POSTGRES_DB=sgca
POSTGRES_USER=sgca_user
POSTGRES_PASSWORD=sgca_password_123
PGPORT=5432

# Backend configuration (OBLIGATORIO: debe coincidir con POSTGRES_* de arriba)
DATABASE_URL=postgresql://sgca_user:sgca_password_123@db:5432/sgca
JWT_SECRET=change_me_change_this_in_prod

# Frontend: URL del API que el NAVEGADOR usará para llamar al backend.
# Con Docker y acceso por localhost usa http://localhost:4000
# En producción usa la URL pública del API (ej. https://tu-api.ejemplo.com)
VITE_API_URL=http://localhost:4000
