# Example environment configuration
# Copia: cp .env.example .env  y ajusta valores.

# PostgreSQL database settings
POSTGRES_DB=bd
POSTGRES_USER=user
POSTGRES_PASSWORD=password
PGPORT=5432

# Backend configuration (OBLIGATORIO: debe coincidir con POSTGRES_* de arriba)
# Con Docker: usa @db como host. Sin Docker: usa localhost.
DATABASE_URL=postgresql://user:password@db:5432/bd
PORT=4000
JWT_SECRET=change_me_change_this_in_prod

# Frontend: URL del API que el NAVEGADOR usará para llamar al backend.
# IMPORTANTE: El navegador NO puede resolver "backend" (solo existe en la red Docker).
# Con Docker local: http://localhost:4000
# En producción: https://tu-api.ejemplo.com
VITE_API_URL=http://localhost:4000

# URL del frontend (para enlaces en emails: recuperar contraseña, etc.)
# Con Docker local: http://localhost:3000
# En producción: https://tu-app.ejemplo.com
FRONTEND_URL=http://localhost:3000

# CORS (opcional): en producción, lista de orígenes permitidos separados por coma
# Ej: CORS_ORIGIN=http://localhost:3000,https://tu-front.ejemplo.com

# === Email (recuperación de contraseña) ===
# Sin SMTP: los emails se encolan y el worker los imprime en logs (solo desarrollo).
# Con SMTP: configurar para enviar emails reales en producción.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-correo@gmail.com
# SMTP_PASS=tu-app-password
# EMAIL_FROM=no-reply@tu-dominio.com
# EMAIL_FROM_NAME=SGCA
